import React from 'react'

export const Header = () => {
  return (
    <header className="container-header">
      <p>header, delete header prob not necessary</p>
    </header>
  )
}

export default Header